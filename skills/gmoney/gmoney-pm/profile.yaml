# gmoney-pm profile
#
# Refinement knobs for this skill. Hermes does not currently apply per-skill
# overrides natively — see scripts/install.py and README "Profiles" for how
# this is consumed today. Treat each value as the recommended setting when
# the orchestrator hands control to this role.

role: pm
model: anthropic/claude-opus-4.6
provider: openrouter
reasoning_effort: medium
max_tokens: 4096
toolsets: []
notes: |
  Synthesis step. Output is a single fenced JSON code block conforming to
  the schema in SKILL.md (positions + narrative, weights summing to 100).
  No tools needed — only the three analyst reports + the thesis. Reasoning
  is medium because the structure is fixed and over-thinking tends to
  produce schema drift. If a future Hermes supports per-skill temperature,
  drop it here (~0.2) for more deterministic JSON.
